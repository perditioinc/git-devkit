# Changelog

## 1.0.0 (2026-05-28)

- Initial standalone release. Generic GitHub tooling - badges, async GitHub client, file + report utilities - extracted into a self-contained package with no external project dependencies. Import namespace `git_devkit`; badge markers `git-devkit-badges-{start,end}`.
